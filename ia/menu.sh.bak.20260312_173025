#!/usr/bin/env bash
set -euo pipefail

BASE="/home/capgalius/skynet"
RETRO="$BASE/retro_ui.sh"

if [ -x "$RETRO" ]; then
  exec "$RETRO"
else
  echo "Interface retro não encontrada: $RETRO"
  echo "Voltar ao menu principal."
  exit 1
fi
